<UserControl x:Class="VerticalTabControlLib.TabUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:VerticalTabControlLib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <SolidColorBrush x:Key="TabItem.Selected.Background" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TabItem.Selected.Border" Color="#ACACAC"/>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <LinearGradientBrush x:Key="TabItem.Static.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="#F0F0F0" Offset="0.0"/>
            <GradientStop Color="#E5E5E5" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="TabItem.Static.Border" Color="#ACACAC"/>
        <LinearGradientBrush x:Key="TabItem.MouseOver.Background" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="#ECF4FC" Offset="0.0"/>
            <GradientStop Color="#DCECFC" Offset="1.0"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="TabItem.MouseOver.Border" Color="#7EB4EA"/>
        <SolidColorBrush x:Key="TabItem.Disabled.Background" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="TabItem.Disabled.Border" Color="#D9D9D9"/>
        <Style x:Key="TabItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Background" Value="{StaticResource TabItem.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource TabItem.Static.Border}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="6,2,6,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                            <Border x:Name="mainBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1,1,1,0" Background="{TemplateBinding Background}" Margin="0">
                                <Border x:Name="innerBorder" BorderBrush="{StaticResource TabItem.Selected.Border}" BorderThickness="1,1,1,0" Background="{StaticResource TabItem.Selected.Background}" Margin="-1" Opacity="0"/>
                            </Border>
                            <Border x:Name="contentBorder" BorderThickness="0" BorderBrush="DarkGreen">
                                <ContentPresenter x:Name="contentPresenter" ContentSource="Content" Focusable="False" HorizontalAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                    <!--<Condition Binding="{Binding TabStripPlacement, RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="Left"/>-->
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="mainBorder" Value="{StaticResource TabItem.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="mainBorder" Value="{StaticResource TabItem.MouseOver.Border}"/>
                                <Setter Property="BorderThickness" TargetName="innerBorder" Value="1,1,0,1"/>
                                <Setter Property="BorderThickness" TargetName="mainBorder" Value="1,1,0,1"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="false"/>
                                    <!--<Condition Binding="{Binding TabStripPlacement, RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="Left"/>-->
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Opacity" TargetName="contentPresenter" Value="0.56"/>
                                <Setter Property="Background" TargetName="mainBorder" Value="{StaticResource TabItem.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="mainBorder" Value="{StaticResource TabItem.Disabled.Border}"/>
                                <Setter Property="BorderThickness" TargetName="innerBorder" Value="1,1,0,1"/>
                                <Setter Property="BorderThickness" TargetName="mainBorder" Value="1,1,0,1"/>
                            </MultiDataTrigger>
                            
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="false"/>
                                    <!--<Condition Binding="{Binding TabStripPlacement, RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="Left"/>-->
                                </MultiDataTrigger.Conditions>
                                <Setter Property="BorderThickness" TargetName="innerBorder" Value="1,1,0,1"/>
                                <Setter Property="BorderThickness" TargetName="mainBorder" Value="1,1,0,1"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="true"/>
                                    <!--<Condition Binding="{Binding TabStripPlacement, RelativeSource={RelativeSource AncestorType={x:Type TabControl}}}" Value="Left"/>-->
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                                <Setter Property="Margin" Value="-2,-2,0,-2"/>
                                <Setter Property="Opacity" TargetName="innerBorder" Value="1"/>
                                <Setter Property="BorderThickness" TargetName="innerBorder" Value="1,1,0,1"/>
                                <Setter Property="BorderThickness" TargetName="mainBorder" Value="1,1,0,1"/>
                                <Setter Property="BorderThickness" TargetName="contentBorder" Value="0,0,0,2"/>
                                <Setter Property="Foreground" Value="DarkGreen"/>
                            </MultiDataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ListBoxStyle" TargetType="{x:Type ListBox}">
            <Setter Property="Padding" Value="1,1,-1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel>
                            <i:Interaction.Behaviors>
                                <i:FluidMoveBehavior AppliesTo="Children" Duration="0:0:0.12"></i:FluidMoveBehavior>
                            </i:Interaction.Behaviors>
                        </StackPanel>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1F2A37"/>
            <Setter Property="Background" Value="#F7F9FC"/>
            <Setter Property="BorderBrush" Value="#C8D1DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="buttonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#EAF2FF"/>
                                <Setter TargetName="buttonBorder" Property="BorderBrush" Value="#8FB4E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#DCE9FB"/>
                                <Setter TargetName="buttonBorder" Property="BorderBrush" Value="#6E97D3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="buttonBorder" Property="Background" Value="#F1F1F1"/>
                                <Setter TargetName="buttonBorder" Property="BorderBrush" Value="#D7D7D7"/>
                                <Setter Property="Foreground" Value="#8A8A8A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="IconButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="MinWidth" Value="40"/>
        </Style>
        <Style x:Key="TextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="2"/>
        </Style>
        <DataTemplate x:Key="SheetItemTemplate">
            <Grid x:Name="itemRoot" Opacity="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Width="12"
                        Margin="0,0,10,0"
                        CornerRadius="4"
                        MinHeight="18"
                        VerticalAlignment="Stretch"
                        Background="{Binding TabColorBrush}"
                        BorderBrush="#B5B5B5"
                        BorderThickness="1"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding Header}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
                <Border x:Name="hiddenBadge"
                        Grid.Column="2"
                        Margin="8,0,0,0"
                        Padding="6,1"
                        Background="#F2F2F2"
                        BorderBrush="#D8D8D8"
                        BorderThickness="1"
                        CornerRadius="8"
                        Visibility="Collapsed">
                    <TextBlock x:Name="hiddenLabel"
                               Text="{Binding VisibilityLabel}"
                               FontSize="10"
                               Foreground="#666666"/>
                </Border>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding VisibilityMode}" Value="{x:Static local:SheetVisibilityMode.Hidden}">
                    <Setter TargetName="itemRoot" Property="Opacity" Value="0.58"/>
                    <Setter TargetName="hiddenBadge" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="hiddenBadge" Property="Opacity" Value="1"/>
                    <Setter TargetName="hiddenBadge" Property="BorderBrush" Value="#BFBFBF"/>
                    <Setter TargetName="hiddenBadge" Property="Background" Value="#EEEEEE"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding VisibilityMode}" Value="{x:Static local:SheetVisibilityMode.VeryHidden}">
                    <Setter TargetName="itemRoot" Property="Opacity" Value="0.42"/>
                    <Setter TargetName="hiddenBadge" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="hiddenBadge" Property="Opacity" Value="1"/>
                    <Setter TargetName="hiddenBadge" Property="BorderBrush" Value="#D4A4A4"/>
                    <Setter TargetName="hiddenBadge" Property="Background" Value="#F7E4E4"/>
                    <Setter TargetName="hiddenLabel" Property="Foreground" Value="#8F3A3A"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <TextBox x:Name="txtFilter"
                         d:DataContext="{d:DesignInstance {x:Type local:TabUserControlViewModelMock}}" 
                         Style="{StaticResource TextBoxStyle}"
                         Text="{Binding InputToFilter, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="#8E97A3"
                           IsHitTestVisible="False"
                           Text="Search by Sheet Name...">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=txtFilter}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=txtFilter}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <Button x:Name="cmdSortMenu"
                    Grid.Column="1"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTip="Sort sheets"
                    Click="CmdSortMenu_OnClick">
                <Button.ContextMenu>
                    <ContextMenu x:Name="ctxSort" Opened="SortContextMenu_OnOpened">
                        <MenuItem x:Name="mnuSortNameAscending" Header="Name A-Z" Click="SortNameAscendingMenuItem_OnClick"/>
                        <MenuItem x:Name="mnuSortNameDescending" Header="Name Z-A" Click="SortNameDescendingMenuItem_OnClick"/>
                    </ContextMenu>
                </Button.ContextMenu>
                <Viewbox Width="16" Height="16" Stretch="Uniform">
                    <Canvas Width="16" Height="16">
                        <Path Data="M3,4.2H10.5 M3,8H9 M3,11.8H7.2"
                              Stroke="#2E5B8A"
                              StrokeThickness="1.6"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"/>
                        <Path Data="M11.4,5.5 L13.9,8 L11.4,10.5"
                              Stroke="#2E5B8A"
                              StrokeThickness="1.6"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"/>
                    </Canvas>
                </Viewbox>
            </Button>
        </Grid>
        
        <local:CustomListBox x:Name="lstTab" 
                 d:DataContext="{d:DesignInstance {x:Type local:TabUserControlViewModelMock}}"
                 Grid.Row="1"
                 Style="{StaticResource ListBoxStyle}"
                 SelectedItem="{Binding SelectedItem}"
                 ItemsSource="{Binding ItemsView}"
                 ItemTemplate="{StaticResource SheetItemTemplate}"
                 ItemContainerStyle="{StaticResource TabItemStyle}"
                 SelectionMode="Extended"
                 EnableSortByDragAndDrop="True"
                 AllowDrop="True"
                 TryMoved="LstTab_OnTryMoved"
                 PreviewKeyDown="LstTab_OnPreviewKeyDown">
            <local:CustomListBox.ContextMenu>
                <ContextMenu Opened="TabContextMenu_OnOpened">
                    <MenuItem x:Name="mnuRename" Header="Rename" Click="RenameMenuItem_OnClick"/>
                    <MenuItem x:Name="mnuDuplicate" Header="Duplicate" Click="DuplicateMenuItem_OnClick"/>
                    <MenuItem x:Name="mnuDelete" Header="Delete" Click="DeleteMenuItem_OnClick"/>
                    <Separator/>
                    <MenuItem x:Name="mnuHide" Header="Hide" Click="HideMenuItem_OnClick"/>
                    <MenuItem x:Name="mnuVeryHide" Header="Very Hidden" Click="VeryHideMenuItem_OnClick"/>
                    <MenuItem x:Name="mnuShow" Header="Show" Click="ShowMenuItem_OnClick"/>
                </ContextMenu>
            </local:CustomListBox.ContextMenu>

        </local:CustomListBox>

        <Grid Grid.Row="2" Margin="4,6,4,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="cmdNew"
                    Grid.Column="0"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTip="New sheet"
                    Click="CmdNew_OnClick">
                <Viewbox Width="14" Height="14" Stretch="Uniform">
                    <Canvas Width="14" Height="14">
                        <Path Data="M7,2 L7,12 M2,7 L12,7"
                              Stroke="#2E5B8A"
                              StrokeThickness="1.8"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"/>
                    </Canvas>
                </Viewbox>
            </Button>
            <Button x:Name="cmdRefresh"
                    Grid.Column="1"
                    Style="{StaticResource IconButtonStyle}"
                    ToolTip="Refresh sheets"
                    Click="CmdRefresh_OnClick">
                <Viewbox Width="15" Height="15" Stretch="Uniform">
                    <Canvas Width="16" Height="16">
                        <Path Data="M13.5,5.8C12.9,3.5 10.8,1.8 8.3,1.8C5.3,1.8 2.8,4.3 2.8,7.3C2.8,10.3 5.3,12.8 8.3,12.8C10.6,12.8 12.6,11.4 13.3,9.4"
                              Stroke="#2E5B8A"
                              StrokeThickness="1.6"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"/>
                        <Path Data="M11.2,2.2H14.1V5.1"
                              Stroke="#2E5B8A"
                              StrokeThickness="1.6"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              StrokeLineJoin="Round"/>
                    </Canvas>
                </Viewbox>
            </Button>
        </Grid>
    </Grid>
</UserControl>
